# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored.

domain_name = "punjabi.example.com"

# aws_region          = "us-west-2"
# availability_zone   = "us-west-2a"
# instance_name       = "punjaber"
# bundle_id           = "micro_3_0"   # verify via: aws lightsail get-bundles --region us-west-2
# blueprint_id        = "ubuntu_22_04"
# disk_size_gb        = 10
# dockerhub_namespace = "valkvlabs"
# image_tag           = "latest"

# Leave false on a public domain: the app has no logins, so uploads would be
# open to anyone who finds the URL.
# enable_studio       = false

# open_ssh            = false
